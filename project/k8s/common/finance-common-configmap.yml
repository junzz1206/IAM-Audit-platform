apiVersion: v1
kind: ConfigMap
metadata:
  name: finance-common-config
  namespace: onprem-apps
data:
  # Spring
  SPRING_PROFILES_ACTIVE: "onprem"
  SERVER_PORT: "8080"

  # Database (Finance)
  DB_HOST: "192.168.1.151"
  DB_PORT: "5432"
  DB_NAME: "finance_db"

  # Schema / Table
  DB_FINANCE_SCHEMA: "finance"
  DB_RAW_TX_TABLE: "card_transactions_raw"
  DB_AUDIT_SCHEMA: "audit"
  DB_AUDIT_TABLE: "audit_events"

