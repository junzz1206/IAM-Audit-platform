apiVersion: v1
kind: Service

metadata:
  name: audit-query-api
  namespace: "onprem-audit"

spec:
  type: ClusterIP

  selector:
    app: audit-query-api

  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

