apiVersion: v1
kind: Service

metadata:
  name: ingest-service
  namespace: onprem-apps
  labels:
    app: ingest-service

spec:
  type: ClusterIP

  selector:
    app: ingest-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

