apiVersion: v1
kind: Service
metadata:
  name: audit-reader
  labels:
    app: audit-reader
spec:
  selector:
    app: audit-reader
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

