apiVersion: v1
kind: Service
metadata:
  name: iam-service
  namespace: onprem-iam
spec:
  selector:
    app: iam-service
  ports:
    - name: http
      port: 80
      targetPort: 8080   # Spring Boot 기본
  type: ClusterIP

