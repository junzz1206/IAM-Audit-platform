# ==========================================================
# NFS Role - Defaults
# ==========================================================

# ----------------------------------------------------------
# NFS server packages (Rocky / RHEL)
# ----------------------------------------------------------
nfs_packages:
  - nfs-utils

# ----------------------------------------------------------
# NFS service name
# ----------------------------------------------------------
nfs_service_name: nfs-server

# ----------------------------------------------------------
# NFS export definitions
# (inventory 또는 play에서 override 가능)
# ----------------------------------------------------------
nfs_exports: []

