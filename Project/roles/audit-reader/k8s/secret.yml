apiVersion: v1
kind: Secret
metadata:
  name: audit-reader-db-secret
type: Opaque

stringData:
  DB_HOST: "{{ db_host }}"
  DB_PORT: "{{ db_port | string }}"
  DB_NAME: "{{ db_name }}"
  DB_USER: "{{ audit_reader_db_user }}"
  DB_PASSWORD: "{{ audit_reader_db_password }}"

