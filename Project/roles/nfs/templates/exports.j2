# ==========================================================
# /etc/exports
# Managed by Ansible (NFS role)
# ==========================================================

{% for export in nfs_exports %}
{{ export.path }} {{ export.clients | default('*') }}({{ export.options | default(nfs_export_options | default('rw,sync,no_root_squash')) }})
{% endfor %}

