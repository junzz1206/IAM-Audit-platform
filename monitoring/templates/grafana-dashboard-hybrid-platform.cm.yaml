apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-hybrid-platform
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  hybrid-platform.json: |-
{{ .Files.Get "dashboards/hybrid-platform.json" | nindent 4 }}
